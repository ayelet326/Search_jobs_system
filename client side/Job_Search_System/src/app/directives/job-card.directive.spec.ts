import { JobCardDirective } from './job-card.directive';

describe('JobCardDirective', () => {
  it('should create an instance', () => {
    const directive = new JobCardDirective();
    expect(directive).toBeTruthy();
  });
});
